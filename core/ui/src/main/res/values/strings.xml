<?xml version="1.0" encoding="utf-8"?>
<!--
    화면 문구는 전량 이 모듈에 모은다. 순수 Kotlin 모듈은 리소스에 접근할 수 없으므로
    이 배치가 곧 "Repository 구현은 UI 문구를 알지 않는다"를 컴파일러가 강제하는 장치가 된다.
-->
<resources>

    <!-- 로그인 -->
    <!-- %1$s·%2$s 는 아래 두 문서 이름이며, 화면에서 밑줄과 링크가 붙는다. -->

    <!-- 법적 문서 -->

    <!-- 메인 탭. 헤더의 섹션 이름과 하단 탭의 라벨에 함께 쓴다. -->
    <string name="main_tab_home">홈</string>
    <string name="main_tab_chat">채팅</string>
    <string name="main_tab_studio">제작</string>
    <string name="main_tab_my">마이</string>

    <!-- 스토리 목록 카드 공통. 홈(오리지널)과 제작(내가 만든) 목록이 함께 쓴다. -->
    <!-- 목록을 보고 있는 중에 당겨서 새로고침한 결과. 목록은 그대로 남으므로 "불러오지 못했어요"와 구분한다. -->
    <!-- 턴 수 뱃지. 숫자만 보이므로 화면 낭독기에는 무엇의 수인지 함께 읽힌다. -->
    <!-- 제작자 닉네임. 선행 @ 는 리소스 참조로 읽히지 않게 이스케이프한다. -->

    <!-- 홈 -->

    <!-- 제작 -->
    <!-- 카드 폭에 다 못 들어간 장르 뱃지의 접힌 개수. -->
    <!-- 메타 값은 숫자·날짜만 보이므로 낭독기에는 무엇인지 함께 읽힌다. -->
    <!-- 카드 옵션 시트. 더보기 버튼과 길게 누르기가 같은 시트를 연다. -->
    <!-- 카드 옵션 시트 · 삭제 확인 다이얼로그 (웹 스토리 옵션 메뉴와 문구 계약 공유) -->
    <!-- 이어서 만들기 배너 · 진입 다이얼로그 (3-1 §3-1-4 문구 계약) -->

    <!-- 간편 제작 퍼널 -->
    <!-- %1$s 는 단계 이름, %2$d·%3$d 는 현재/전체 단계 수. 인디케이터의 접근성 텍스트에만 쓴다. -->
    <!-- %1$s 는 카테고리 탭 라벨(예: 장르). -->
    <!-- 주변 인물 이름 placeholder. 입력 섹션 순서대로 순환한다. -->
    <!-- 스토리라인 탭의 순번 라벨. 생성 결과 순서대로 대응한다. -->
    <!-- 로딩 중 타자기형으로 순환하는 문구. -->
    <!-- 생성이 길어질 때 힌트 위에 뜨는 경과 구분선 문구. -->
    <!-- CTA 위 비용 행. 값은 웹과 같은 완성 비용 정책을 따른다. -->
    <!-- 로딩 중 타자기형으로 순환하는 문구. -->
    <!-- 소실 경고 다이얼로그 (3-1 §3-1-4 이탈 가드 문구 계약) -->
    <!-- 잃을 것은 없지만 닫기 자체를 확인하는 경고. 임시 저장본·진행 중 생성 모두 이어서 만들기로 돌아온다. -->
    <!-- 추가 정보 입력 placeholder. 입력 순서대로 순환한다. 인물 정보는 키워드 단계에서 받으므로 예시에서 뺀다. -->

    <!-- 마이 -->
    <!-- 이프 충전의 두 탭. 이프를 얻는 수단은 무료 충전이, 쓰고 받은 기록은 내역이 맡는다. -->
    <!-- %1$s 는 YYYY-MM-DD 만료일. 획득은 만료 예정일, 소멸은 회수된 로트의 실제 만료일이다. -->
    <!-- %1$s 는 천 단위 콤마를 넣은 절대값. 부호는 획득·소모 분류가 정한다. -->
    <!-- 서버가 사유를 늘렸을 때의 폴백. 금액과 날짜만으로도 줄이 읽히게 한다. -->
    <!-- 700 은 출시 이벤트 값이다. 정책이 바뀌면 서비스 안내와 함께 고친다. -->
    <!-- 라벨 아래에 붙어 "친구 초대 하고 N 이프 받기"로 이어 읽힌다. N 은 서버 정책의 초대 보상이다. -->

    <!-- 계정 연동 -->
    <!-- %1$s 는 연동할 제공자 이름. -->
    <!-- %1$s·%2$s 는 두 제공자 이름이며 카카오·Google 순서로 고정한다. -->
    <!-- %1$s 는 다시 인증할 현재 제공자, %2$s 는 새로 연동할 제공자. -->

    <!-- 신규 가입 초대 코드 안내 -->

    <!-- 마이 하위 화면의 폼 공통 -->

    <!-- 친구 초대 -->
    <!-- %1$d 는 이번 달 받은 보상 횟수, %2$d 는 월 상한. 상한은 정책 값이라 서버 응답이 싣는다. -->
    <!-- %1$s 는 내 초대 코드, %2$s 는 웹 주소. 공유 시트에 실리는 본문이다. -->

    <!-- 회원 탈퇴 -->

    <!-- 피드백 -->

    <!-- 오픈소스 라이선스 -->

    <!-- 세션 종료 안내 -->
    <string name="session_cleanup_failed">이전 로그인 정보를 정리하지 못했어요. 다시 시도해주세요</string>

    <!-- 스토리 상세 -->
    <!-- 엔딩 라벨 옆 안내 버튼과 그 버튼이 여는 말풍선. -->
    <!-- 시작 설정이 둘 이상일 때만 나오는 선택 트리거와 그 바텀 시트. -->


    <!-- 스토리 신고. 상세와 채팅방이 같은 시트를 쓰며, 신고 대상은 두 곳 모두 스토리다. -->

    <!-- 채팅 목록 -->
    <!-- 웹은 스토리 보유 여부로 안내를 가르지만, 앱에서는 제작 탭이 그 갈래를 이미 안에 담고 있다. -->
    <!-- 완료된 턴이 없는 채팅(생성 직후)의 미리보기 자리. -->
    <!-- 참조 스토리가 삭제된 채팅의 제목 자리. 스토리는 사라져도 채팅은 계속 열 수 있다. -->
    <!-- 참조 스토리가 삭제된 채팅. 목록 카드 제목과 채팅방 앱바 제목이 같은 문구를 쓴다. -->
    <!-- 카드 하나가 링크 하나다. 제목·미리보기·메타는 카드 안에서 이미 읽히므로 동작 이름만 붙인다. -->
    <!-- 카드를 길게 누르면 여는 옵션 시트. -->
    <!-- 메타 값은 숫자만 보이므로 낭독기에는 무엇의 수·시각인지 함께 읽힌다. -->

    <!-- 상대 시간. 웹과 같은 경계에서 바뀌어야 같은 채팅이 두 클라이언트에서 같은 시각으로 읽힌다. -->

    <!-- 채팅방 -->
    <!-- "작성 중" 대기 문구를 스크린 리더가 읽을 때의 설명. -->
    <!-- %1$s 자리에 채우기 버튼 아이콘이 들어간다. -->
    <!-- %1$s 는 도달한 엔딩 이름. -->

    <!-- 채팅방 컴포저 -->
    <!-- 응답 생성 중 잠긴 입력창을 눌렀을 때. 왜 안 되는지와 언제 되는지를 한 문장에 담는다. -->
    <!-- 전송 버튼 왼쪽 턴 비용. 값은 웹과 같은 채팅 턴 비용 정책을 따른다. -->

    <!-- 오류 -->

    <!-- 공통 -->
    <!-- %1$d 는 현재 글자 수, %2$d 는 상한. 입력 상한이 있는 모든 입력창이 함께 쓴다. -->
</resources>
